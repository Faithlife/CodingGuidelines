# AGENTS

Repository instructions for automated coding agents.

- In PowerShell scripts and tests, do not use hyphens in user-defined function names.
- Name user-defined PowerShell functions in PascalCase and use approved PowerShell verbs, for example `NewTestDirectory` and `TestConformingGlobalJson`.
- Apply the same rule when renaming or adding helper functions in convention scripts and Pester tests.