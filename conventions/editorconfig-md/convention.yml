commit:
  message: Update .editorconfig for Markdown

conventions:
  - path: ../editorconfig-section
    settings:
      name: md
      text: ${{ readText("files/.editorconfig") }}

pull-request:
  auto-merge: true
