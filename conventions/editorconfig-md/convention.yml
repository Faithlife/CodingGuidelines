conventions:
  - path: ../editorconfig
    settings:
      name: md
      text: ${{ readText(".editorconfig") }}
      agent:
        instructions: ${{ readText("agent-instructions.md") }}
      commit:
        message: "Update Markdown editorconfig settings."
