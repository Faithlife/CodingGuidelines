commit:
  message: "Update .editorconfig for JSON"

conventions:
  - path: ../editorconfig-section
    settings:
      name: json
      text: ${{ readText("files/.editorconfig") }}

pull-request:
  auto-merge: true
