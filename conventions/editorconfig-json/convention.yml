commit:
  message: "Update JSON editorconfig settings"

conventions:
  - path: ../editorconfig-section
    settings:
      name: json
      text: ${{ readText("files/.editorconfig") }}

pull-request:
  auto-merge: true
