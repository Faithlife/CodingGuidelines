# apm-install-updates

This convention runs `apm install --update` in the target repository.

It can be used to install missing APM-managed dependencies and update existing ones.

```yaml
conventions:
- path: Faithlife/CodingGuidelines/conventions/apm-install-updates
```
