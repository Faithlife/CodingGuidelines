conventions:
  - path: ../editorconfig
    settings:
      name: csharp
      text: ${{ readText(".editorconfig") }}
      agent:
        instructions: ${{ readText("agent-instructions.md") }}
      commit: ${{ settings.commit }}