commit:
  message: "Update .editorconfig for C#"

conventions:
  - path: ../editorconfig-section
    settings:
      name: csharp
      text: ${{ readText("files/.editorconfig") }}

pull-request:
  auto-merge: true
