commit:
  message: Update C# editorconfig settings

conventions:
  - path: ../editorconfig-section
    settings:
      name: csharp
      text: ${{ readText("files/.editorconfig") }}

pull-request:
  auto-merge: true
