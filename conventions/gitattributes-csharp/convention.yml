commit:
  message: "Update C# gitattributes settings"

conventions:
  - path: ../gitattributes-section
    settings:
      name: csharp
      text: ${{ readText("files/.gitattributes") }}

pull-request:
  auto-merge: true