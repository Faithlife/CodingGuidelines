commit:
  message: Update .gitattributes for C#

conventions:
  - path: ../gitattributes-section
    settings:
      name: csharp
      text: ${{ readText("files/.gitattributes") }}

pull-request:
  auto-merge: true