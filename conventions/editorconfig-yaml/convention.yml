conventions:
  - path: ../editorconfig
    settings:
      name: yaml
      text: ${{ readText("files/.editorconfig") }}
      agent:
        instructions: ${{ readText("agent-instructions.md") }}
      commit:
        message: "Update YAML editorconfig settings."
