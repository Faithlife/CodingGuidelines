conventions:
  - path: ../config-text-section
    settings:
      path: .editorconfig
      name: ${{ settings.name }}
      text: ${{ settings.text }}
      comment-prefix: '#'
      agent: ${{ settings.agent }}
      commit: ${{ settings.commit }}
