conventions:
  - path: ../config-text
    settings:
      path: .gitignore
      lines: ${{ settings.lines }}
      section:
        name: ${{ settings.name }}
        text: ${{ settings.text }}
        comment-prefix: '#'
      agent: ${{ settings.agent }}
      commit: ${{ settings.commit }}
