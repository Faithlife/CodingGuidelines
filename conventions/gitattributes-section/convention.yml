conventions:
  - path: ../config-text-section
    settings:
      path: .gitattributes
      name: ${{ settings.name }}
      text: ${{ settings.text }}
      comment-prefix: '#'
      agent: ${{ settings.agent }}
      commit: ${{ settings.commit }}
