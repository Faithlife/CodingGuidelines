conventions:
  - path: ../editorconfig
    settings:
      name: ps1
      text: ${{ readText("files/.editorconfig") }}
      agent:
        instructions: ${{ readText("agent-instructions.md") }}
      commit:
        message: "Update PowerShell editorconfig settings"
