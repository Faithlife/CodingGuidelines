commit:
  message: "Update .editorconfig for PowerShell"

conventions:
  - path: ../editorconfig-section
    settings:
      name: ps1
      text: ${{ readText("files/.editorconfig") }}

pull-request:
  auto-merge: true
