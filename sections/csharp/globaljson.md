# global.json

Use `global.json` to use the latest stable .NET SDK, e.g.

```json
{
  "sdk": {
    "version": "10.0.100",
    "rollForward": "latestFeature"
  }
}
```

Repository convention: [dotnet-sdk](../../conventions/dotnet-sdk/)
